/* global nonic, robotic, automaton, attachInline */

luka.addEventListener(nonic.ai.luka.nerve);
nonic.ai.luka.ids = ['luka'];
(function($start) {  // Avoid conflicts with other libraries
	var $luka = $('#nonic','#ai','#luka','#nerve');
	nonic.lukaNerve = function(luka ,string) {
	if (lukaNerve !== null) {
		status(lukaNerve);
		lukaNerve = null;
	}
	return luka;
  };
 
})(jQuery);