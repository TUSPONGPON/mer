/* global nonic, robotic, automaton, attachInline */

miku.addEventListener(nonic.ai.miku.nerve);
nonic.ai.miku.ids = ['miku'];
(function($start) {  // Avoid conflicts with other libraries
	var $miku = $('#nonic','#ai','#miku','#nerve');
	nonic.mikuNerve = function(miku ,string) {
	if (mikuNerve !== null) {
		status(mikuNerve);
		mikuNerve = null;
	}
	return aria;
  };
 
})(jQuery);